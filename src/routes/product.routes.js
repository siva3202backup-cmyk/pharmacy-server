const r=require('express').Router(),c=require('../controllers/product.controller'),auth=require('../middleware/auth'),admin=require('../middleware/admin');r.get('/',c.list);r.get('/:id',c.get);r.post('/',auth,admin,c.create);r.put('/:id',auth,admin,c.update);r.delete('/:id',auth,admin,c.remove);module.exports=r;
