const db=require('../config/db');exports.sales=async(req,res,next)=>{try{const y=Number(req.query.year)||new Date().getFullYear();const r=await db.query('select extract(month from created_at)::int month,sum(total)::numeric(10,2) total from orders where extract(year from created_at)=$1 group by 1 order by 1',[y]);res.json({year:y,months:r.rows})}catch(e){next(e)}};
